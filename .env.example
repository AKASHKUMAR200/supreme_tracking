# MongoDB Connection
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority

# Example:
# MONGODB_URI=mongodb+srv://admin:password123@cluster0.abc123.mongodb.net/jewellery_tracker?retryWrites=true&w=majority

# Make sure to:
# 1. Replace <username> with your MongoDB username
# 2. Replace <password> with your MongoDB password
# 3. Replace <cluster> with your cluster address
# 4. Replace <database> with your database name
# 5. Whitelist your IP in MongoDB Atlas Network Access
